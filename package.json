{
  "scripts": {
    "cypress-open-digidok": "cypress open --browser chrome"
  },
  "devDependencies": {
    "@cypress/webpack-preprocessor": "^5.15.7",
    "cypress": "^12.17.3",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^15.1.5",
    "ci": "^2.2.0",
    "cypress-network-idle": "^1.14.2",
    "ts-loader": "^9.4.2"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": true,
    "stepDefinitions": "./cypress/integration/**/*.steps.ts",
    "cucumberJson": {
      "generate": true,
      "outputFolder": "./cucumber"
    }
  }
}
